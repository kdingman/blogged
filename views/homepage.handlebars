<div class="row">
    <div class="col-auto">
        <h2> Hello {{user.username}}! </h2>
    </div>
</div>
<br>
{{#if posts.length}}
<div class="card flex-column">
    {{#each posts as |post|}}
    {{> post-info post}}
    {{/each}}
</div>
{{/if}}