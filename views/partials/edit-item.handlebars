<div class="card-body">
    <a href="/dashboard/edit/{{id}}">
    <div class="card-header flex-row justify-space-between">
        <div class="col-8">Title: {{title}}</div>
        <div class="col-4">Posted By {{user.username}} on {{format_date created_at}}</div>
    </div>
    </a>
</div>