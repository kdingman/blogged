<div class="row">
        <h2> Welcome to #Blogged! </h2>
    </div>
</div>
<br>
{{#if posts.length}}
<div class="card flex-column">
    {{#each posts as |post|}}
    {{> post-info post}}
    {{/each}}
</div>
{{/if}}